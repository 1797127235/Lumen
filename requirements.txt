fastapi>=0.136.0
uvicorn>=0.47.0
pydantic>=2.7.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9

sqlalchemy>=2.0.0
aiosqlite>=0.20.0

openai>=1.30.0


python-jose[cryptography]>=3.3.0
httpx>=0.27.0

python-dotenv>=1.0.0
orjson>=3.10.0
pyyaml>=6.0
tenacity>=8.2.0
loguru>=0.7.0
structlog>=24.1.0

# AI 中台
tiktoken>=0.7.0

# Agent
pydantic-ai==1.89.1

# 简历解析 + LLM 统一层
markitdown>=0.0.1
litellm>=1.83.7

# 记忆层：Cognee + Kuzu
cognee==1.0.5
kuzu==0.11.3

# 外部数据接入：文件系统监听
watchdog>=4.0.0

# Phase 4 — PDF / HTML Parser
pypdf>=5.0.0
beautifulsoup4>=4.12.0
docling>=2.94.0

# MCP
mcp>=1.12.0

# Web 工具（可选，增强搜索/提取能力）
duckduckgo-search>=6.0.0   # 免费搜索后端
# firecrawl>=1.0.0         # Firecrawl 提取（可选，需 API Key）
# exa-py>=1.0.0            # Exa 搜索（可选，用 httpx 直接调也可）

# Telegram Bot
python-telegram-bot>=22.0
telegramify-markdown>=1.1.0

# CLI 交互界面（独立应用：python -m lib.channels.cli）
rich>=13.0.0
prompt_toolkit>=3.0.0

# 依赖冲突修复：limits 4.4.1~4.8.x 限制 packaging<25，与 xai-sdk 冲突
# limits 5.0.0+ 已解除上限
limits>=5.0.0

# Dev / CI
ruff==0.15.8
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
