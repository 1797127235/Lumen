# 技术栈选型与数据架构

> 源自：系统架构设计_AI职业规划智能体.md

## 3. 技术栈选型

### 3.1 前端技术栈

| 端 | 技术方案 | 版本 | 选型理由 |
|---|---------|------|---------|
| **Web PC** | React 18 + TypeScript 5.x + Next.js 14 | React 18.3 | SSR/SSG优化首屏加载，SEO友好；组件生态成熟；支持流式渲染（AI流式输出适配） |
| | 状态管理: Zustand | 4.5 | 轻量，比Redux更适合中小项目，支持异步流 |
| | UI组件: Ant Design 5.x | 5.18 | 企业级组件库，表单/表格/图表组件丰富 |
| | 图表: ECharts 5 / D3.js | 5.4 | 雷达图、桑基图、时间线图等复杂图表支持 |
| | 流式渲染: SSE/EventSource | - | AI对话流式输出标准方案，比WebSocket更轻量 |
| **移动端H5** | React + Taro 3.x | Taro 3.6 | 一套代码编译多端（H5+小程序），开发效率最大化 |
| **微信小程序** | Taro 3.x + React | Taro 3.6 | 复用H5代码，支持微信原生API（订阅消息/扫码/相册） |
| | 小程序UI: taro-ui | 3.1 | Taro官方UI库，适配移动端交互 |

**跨端策略**：核心逻辑（状态管理、API调用）封装为独立 npm 包，三端共享。UI层各端独立适配。

### 3.2 后端技术栈

| 维度 | 技术方案 | 版本 | 选型理由 |
|------|---------|------|---------|
| **编程语言** | Python 3.11 | 3.11.7 | AI/ML生态无可替代（LangChain、Transformers、Pandas）；Agent编排、RAG、模型调用全Python栈 |
| | Go 1.22 | 1.22 | 高性能服务（求职进度管理、通知推送）；并发处理优秀；编译部署简单 |
| **Web框架** | Python: FastAPI | 0.111 | 异步原生支持（ASGI）、自动OpenAPI文档、类型提示、性能接近Go |
| | Go: Gin | 1.9 | 高性能、中间件生态成熟、国内社区活跃 |
| **API网关** | Kong (Open Source) | 3.6 | 开源、插件丰富（认证/限流/日志/转换）、支持Lua自定义插件、性能优秀 |
| **任务队列** | Celery + Redis Broker | 5.3 | Python生态标准方案，支持定时任务/延迟任务/重试机制 |
| | | | Go侧使用 Kafka Consumer 处理异步消息 |
| **ORM/数据库** | SQLAlchemy 2.0 (Python) | 2.0 | 支持异步、类型提示、Alembic迁移 |
| | GORM (Go) | 1.25 | Go生态最成熟的ORM |
| **文档处理** | PyMuPDF (PDF解析) | 1.24 | 简历PDF解析 |
| | python-docx (Word解析) | 1.1 | 简历Word解析 |
| | PaddleOCR (OCR) | 2.7 | 拍照简历识别、中文场景优秀 |

### 3.3 AI / LLM 层技术栈

| 维度 | 技术方案 | 选型理由 |
|------|---------|---------|
| **Agent框架** | LangChain 0.2 | 生态最成熟，Chain/Agent/LCEL抽象清晰，社区活跃，文档完善 |
| | LlamaIndex (备选) | 如果RAG需求更复杂（如多模态、GraphRAG），可补充使用 |
| **模型路由** | 自研路由层 | 根据任务类型+成本预算+合规要求，自动选择模型 |
| | | 策略：创意对话→DeepSeek-V3；逻辑推理→Claude 3.5；国内合规→通义千问 |
| **Embedding** | BAAI/bge-large-zh-v1.5 | 中文Embedding SOTA级，开源可本地部署，1024维 |
| | | 备选：智谱 Embedding-3 |
| **Rerank** | BAAI/bge-reranker-large | 重排序模型，提升检索精度 |
| **向量数据库** | Milvus 2.3 | 分布式向量数据库，十亿级支持，云原生，社区活跃 |
| | | 备选：Qdrant（轻量，单节点）|
| **提示工程** | PromptLayer / LangSmith | Prompt版本管理、A/B测试、效果追踪 |
| | | MVP阶段可用Git管理，逐步引入专业工具 |
| **内容安全** | 自研规则引擎 + 阿里云内容安全 | 关键词过滤 + AI调用阿里云绿网API |

#### 多模型路由策略

```python
# 模型路由决策矩阵
ROUTING_MATRIX = {
    # 任务类型 -> (默认模型, 备选模型, 成本上限)
    "general_chat":       ("deepseek-v3",    "qwen-max",     0.005),  # 元/1k tokens
    "career_planning":    ("claude-3.5-sonnet", "gpt-4o",    0.015),
    "resume_optimize":    ("gpt-4o",         "claude-3.5",   0.015),
    "mock_interview":     ("claude-3.5-sonnet", "gpt-4o",    0.02),
    "skill_analysis":     ("deepseek-v3",    "gpt-4o-mini",  0.005),
    "path_generation":    ("claude-3.5-sonnet", "gpt-4o",    0.015),
    "content_safety":     ("gpt-4o-mini",    "qwen-turbo",   0.001),
    "memory_summarize":   ("deepseek-v3",    "qwen-turbo",   0.002),
}
```

**路由规则**：
1. **按任务类型**：不同任务对模型能力要求不同（创意/推理/编码/安全）
2. **按用户等级**：免费用户→轻量模型；付费用户→高端模型
3. **按合规要求**：国内用户→国产模型；海外用户→GPT/Claude
4. **按成本配额**：接近成本上限时自动降级到轻量模型
5. **按模型可用性**：主模型故障时自动切换到备选模型（熔断机制）

### 3.4 数据库与存储

| 类型 | 技术方案 | 版本 | 用途 | 选型理由 |
|------|---------|------|------|---------|
| **关系数据库** | PostgreSQL 16 | 16.2 | 主数据库：用户/对话/路径/简历/求职 | 开源、功能强大、JSONB支持灵活字段、全文检索、成熟稳定 |
| | (阿里云RDS) | | | 托管服务，自动备份、监控、高可用 |
| **向量数据库** | Milvus | 2.3.15 | RAG知识库、简历向量化、记忆检索 | 分布式、GPU加速、Partition机制适合多租户 |
| **缓存** | Redis 7 | 7.2 | 会话状态、热点数据、限流计数、分布式锁 | 单线程高性能、数据结构丰富、持久化选项 |
| **消息队列** | Apache Kafka | 3.6 | 事件流：推送通知、日志、用户行为 | 高吞吐、持久化、可回溯、生态成熟 |
| | Redis Streams | 7.2 | 轻量消息场景：任务分发 | 减少组件数量，简化运维 |
| **对象存储** | MinIO / 阿里云OSS | - | 简历文件、图片、生成的报告PDF | S3兼容、CDN友好、低成本 |
| **时序/分析** | ClickHouse | 24.3 | 用户行为分析、对话质量分析 | 列式存储、聚合查询性能极佳 |
| **搜索引擎** | Elasticsearch 8 | 8.12 | 日志检索、面经全文搜索 | 日志聚合、ELK生态 |

### 3.5 基础设施

| 维度 | 技术方案 | 选型理由 |
|------|---------|---------|
| **云服务商** | 阿里云（主） | 国内合规（ICP备案）、学生用户在国内、延迟低、ACK托管K8s成熟 |
| | 腾讯云（备选） | 微信生态集成友好（小程序/微信支付） |
| **容器编排** | Kubernetes 1.29 (阿里云ACK) | 行业标准，弹性伸缩（HPA/VPA），服务发现，配置管理 |
| **服务网格** | 暂不引入 | MVP阶段K8s原生Service足够，Istio增加复杂度 |
| **CI/CD** | GitHub Actions + ArgoCD | GitHub Actions构建镜像 → ArgoCD GitOps部署到ACK |
| **镜像仓库** | Harbor 2.10 | 自托管，安全扫描，多租户隔离 |
| **配置管理** | Helm 3 + ConfigMap/Secret | Helm Chart管理K8s资源配置，Secret加密存储敏感信息 |
| **密钥管理** | 阿里云KMS + External Secrets | 云KMS管理根密钥，External Secrets Operator同步到K8s |

---

## 4. 数据架构设计

### 4.1 核心数据模型

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         【核心数据实体关系】                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐    │
│   │  user    │────<│user_profile│     │learning_ │     │ resume   │    │
│   │ 用户表   │ 1:1 │ 用户画像  │     │  path    │     │ 简历表   │    │
│   │          │     │          │<1:N│ 学习路径  │<1:N│          │    │
│   └────┬─────┘     └──────────┘     │          │     └────┬─────┘    │
│        │                              └──────────┘          │          │
│        │ 1:N                                               1:N         │
│        ▼                                                   ▼          │
│   ┌──────────┐                                        ┌──────────┐   │
│   │conversation│                                    │resume_section│   │
│   │ 对话会话   │<1:N                                  │ 简历段落   │   │
│   │          │                                        │          │   │
│   └────┬─────┘                                        └──────────┘   │
│        │ 1:N                                                         │
│        ▼                                                              │
│   ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐    │
│   │ message  │     │ job_     │     │interview_│     │ assessment│   │
│   │ 消息表   │     │application│    │  record  │     │ 评估记录   │   │
│   │          │     │ 求职申请  │     │ 面试记录  │     │          │   │
│   └──────────┘     └──────────┘     └──────────┘     └──────────┘    │
│                                                                         │
│   其他实体: notification, learning_path_node, skill_node,             │
│             path_progress, calendar_event, memory_summary,            │
│             memory_vector, tool_call_log, model_request_log             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 关键表结构设计

**用户画像表 (`user_profiles`)**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID PK | 用户画像ID |
| `user_id` | UUID FK | 关联用户 |
| `basic_info` | JSONB | 基础信息（学校、专业、年级、学历） |
| `career_goal` | JSONB | 职业目标（方向、目标公司层级、期望城市） |
| `skill_matrix` | JSONB | 技能矩阵（技术→熟练度/经验值） |
| `learning_progress` | JSONB | 当前学习进度摘要 |
| `job_status` | JSONB | 求职状态摘要 |
| `preferences` | JSONB | 个人偏好（解释深度、交互风格、提醒时间） |
| `created_at/updated_at` | TIMESTAMP | 时间戳 |

**对话表 (`conversations`)**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID PK | 会话ID |
| `user_id` | UUID FK | 用户 |
| `title` | VARCHAR | 会话标题（AI自动生成） |
| `agent_type` | ENUM | 路由的Agent类型 |
| `intent` | VARCHAR | 识别意图 |
| `summary` | TEXT | 会话摘要（结束后生成） |
| `embedding` | VECTOR | 会话语义向量（用于相似检索） |
| `status` | ENUM | active / archived / deleted |
| `created_at/closed_at` | TIMESTAMP | 时间戳 |

**消息表 (`messages`)**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID PK | 消息ID |
| `conversation_id` | UUID FK | 会话 |
| `role` | ENUM | user / assistant / system / tool |
| `content` | TEXT | 消息内容 |
| `content_type` | ENUM | text / markdown / structured / image |
| `structured_data` | JSONB | 结构化输出（如雷达图数据、路径JSON） |
| `model_used` | VARCHAR | 使用的模型 |
| `tokens_input/output` | INT | Token消耗 |
| `latency_ms` | INT | 响应延迟 |
| `tool_calls` | JSONB | 调用的工具列表 |
| `feedback` | JSONB | 用户反馈（点赞/点踩/评论） |
| `created_at` | TIMESTAMP | 时间戳 |

**学习路径表 (`learning_paths`)**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID PK | 路径ID |
| `user_id` | UUID FK | 用户 |
| `title` | VARCHAR | 路径标题 |
| `target_direction` | VARCHAR | 目标方向 |
| `target_company_tier` | VARCHAR | 目标公司层级 |
| `start_date/end_date` | DATE | 起止日期 |
| `nodes` | JSONB | 路径节点树（嵌套JSON） |
| `status` | ENUM | active / completed / abandoned |
| `version` | INT | 版本号（动态调整时递增） |

**学习节点表 (`learning_path_nodes`)**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID PK | 节点ID |
| `path_id` | UUID FK | 路径 |
| `node_name` | VARCHAR | 节点名称 |
| `node_type` | ENUM | skill / project / milestone / resource |
| `description` | TEXT | 描述 |
| `expected_duration_days` | INT | 预计天数 |
| `prerequisites` | UUID[] | 前置节点（DAG依赖） |
| `resources` | JSONB | 推荐资源列表 |
| `completion_criteria` | TEXT | 验收标准 |
| `status` | ENUM | pending / in_progress / completed / skipped |
| `completed_at` | TIMESTAMP | 完成时间 |

**简历表 (`resumes`)**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID PK | 简历ID |
| `user_id` | UUID FK | 用户 |
| `original_file` | VARCHAR | 原始文件路径 |
| `parsed_content` | JSONB | 解析后的结构化内容 |
| `skills_vector` | VECTOR | 技能关键词向量 |
| `ats_score` | INT | ATS友好度评分 |
| `version` | INT | 版本号 |

**求职申请表 (`job_applications`)**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID PK | 申请ID |
| `user_id` | UUID FK | 用户 |
| `company` | VARCHAR | 公司 |
| `position` | VARCHAR | 岗位 |
| `jd_text` | TEXT | JD原文 |
| `jd_parsed` | JSONB | 解析后的JD要求 |
| `resume_version` | INT | 使用的简历版本 |
| `match_score` | FLOAT | 匹配度评分 |
| `stage` | ENUM | interested / applied / oa / phone / interview1 / interview2 / interview3 / hr / offer / rejected / withdrew |
| `stage_history` | JSONB | 阶段变更历史 |
| `reminders` | JSONB | 提醒设置 |

### 4.2 数据流设计

```
┌──────────────────────────────────────────────────────────────────────────┐
│                          【数据流全景图】                                  │
└──────────────────────────────────────────────────────────────────────────┘

【用户行为数据流】
用户操作 → Kong网关 → 应用服务 → PostgreSQL(写) → Redis(读缓存)
                                              ↓
                                         Kafka(事件)
                                              ↓
                                    ┌─────────┴─────────┐
                                    ▼                 ▼
                               ClickHouse         实时分析
                              (批量分析)           (Flink)
                                    ↓                 ↓
                              报表/洞察          实时推荐

【对话数据流】
用户提问 → 对话服务 → Agent编排 → LLM路由 → 外部LLM API
                              ↓          ↓
                           RAG检索    模型响应
                           (Milvus)      ↓
                              ↓      流式返回用户
                           记忆读取      ↓
                           (Redis)   消息存储
                              ↓      (PostgreSQL)
                           提示组装      ↓
                              ↓      记忆更新
                           LLM调用      ↓
                              ↓      摘要生成
                           记忆写入      ↓
                        (PostgreSQL) 向量存储
                                      (Milvus)

【简历解析数据流】
用户上传 → 简历服务 → 文件存储(MinIO)
              ↓
         Celery异步任务
              ↓
         ┌──┴──┬────────┐
         ▼     ▼        ▼
      OCR   NLP解析   向量化
    (PaddleOCR)(LLM)(Embedding)
         │     │        │
         └──┬──┴────────┘
            ↓
      PostgreSQL(写入)
            ↓
      触发JD匹配(如有目标岗位)
            ↓
      返回解析结果给用户

【主动推送数据流】
定时任务(Celery Beat) / 事件(Kafka)
         ↓
    通知服务(Go)
         ↓
    ┌────┴────┬─────────┐
    ▼         ▼         ▼
 微信模板   短信网关   站内信
  消息      (阿里云)   (WebSocket)
    ↓         ↓         ↓
  用户      用户       用户
```

### 4.3 向量数据库设计

#### Embedding 策略

| 数据类型 | 文本来源 | Embedding模型 | 维度 | 更新策略 |
|---------|---------|-------------|------|---------|
| **知识库文档** | 知识段落 | bge-large-zh-v1.5 | 1024 | 增量更新（新文档）+ 全量重建（季度） |
| **岗位JD** | JD标题+描述+要求 | bge-large-zh-v1.5 | 1024 | 日度批量更新 |
| **用户简历技能** | 技能关键词+项目描述 | bge-large-zh-v1.5 | 1024 | 用户更新简历时实时更新 |
| **对话历史** | 对话摘要 | bge-large-zh-v1.5 | 1024 | 对话结束时异步生成 |
| **面经** | 面经结构化内容 | bge-large-zh-v1.5 | 1024 | 日度增量 |

#### 分片与索引策略

```yaml
# Milvus Collection 配置示例
collections:
  career_knowledge:
    shard_num: 4                    # 按数据量分4片
    partition_by: category          # 按方向/技术栈分区
    vector_index:
      type: IVF_FLAT               # 平衡精度与性能
      nlist: 1024                  # 聚类中心数
      metric_type: COSINE          # 余弦相似度
    scalar_indexes:
      - field: category
      - field: level              # beginner/intermediate/advanced
      - field: updated_at
    
  user_memory:
    shard_num: 8                    # 用户量大，多分片
    partition_by: user_id % 100    # 按用户ID取模分区
    vector_index:
      type: HNSW                   # 高速检索
      M: 16
      efConstruction: 200
      metric_type: COSINE
```

#### 简历与岗位匹配向量化方案

```
【简历向量化】
简历文本 → 分段（基本信息/教育/技能/项目/实习）
  → 技能段提取关键词（Java, Spring Boot, Redis, MySQL...）
  → 项目段生成摘要
  → 整体生成技能描述文本
  → Embedding → 用户技能向量 (1024维)

【JD向量化】
JD文本 → 解析（岗位名称/公司/技能要求/经验要求）
  → 技能要求文本化（"需要掌握Java、Spring Boot、分布式系统"）
  → Embedding → 岗位需求向量 (1024维)

【匹配计算】
匹配度 = cosine_similarity(用户技能向量, 岗位需求向量) * 0.6
       + 关键词精确匹配率 * 0.3
       + 经验年限匹配 * 0.1

结果：匹配度评分 (0-100) + 缺失技能列表 + 建议补充项
```
