[project]
name = "lumen-rss-mcp"
version = "0.1.0"
description = "Lumen RSS MCP Server — 独立的 RSS 订阅源管理插件"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.12.0",
    "feedparser>=6.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
lumen-rss-mcp = "server:main"
